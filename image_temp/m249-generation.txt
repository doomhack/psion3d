Generated with the built-in image_gen tool.

Initial prompt:
Use case: stylized-concept. Generate an M249-style light machine gun two-frame first-person weapon sprite sheet for a monochrome Psion raycaster. Reference image 1 is the existing PPK idle sprite, image 2 the MP5, image 3 the AK47. Match their low-resolution monochrome pixel-art style and lower-right first-person composition. Exactly TWO equal square cells side by side, idle left and firing right. Intended logical resolution is 128x64 total, each cell 64x64. Depict a recognisable M249 light machine gun: broad boxy receiver and top feed cover, chunky ribbed fore-end, long barrel with raised front sight, folded bipod alongside barrel, and a compact ammunition box visible beneath the receiver. Make its silhouette visibly heavier and broader than an assault rifle while leaving ample empty upper space. View from behind weapon, hands and gun enter from bottom right, barrel aims diagonally toward upper-left/centre. NOT a side-on inventory icon. Identical scale, hand placement, angle and lower-edge anchoring in both frames. Firing frame adds a compact white/grey muzzle flash and minimal recoil. Coarse crisp pixel clusters readable at 64x64. Strict object palette black #000000, medium grey #808080, white #FFFFFF; hands also monochrome. BACKGROUND MUST BE FLAT PURE RED #FF0000, the engine transparency key, including empty gaps and holes. No red on weapon or hands. No checkerboard, gradients, antialiasing, fine high-resolution detailing, text, labels, borders, extra cells, scenery, or shadows outside silhouettes. Two-cell sprite sheet, no mockup.

Final correction prompt:
Correct image 1's layout to match image 2's landscape two-cell sprite-sheet layout. Output a wide 2:1 landscape canvas, exactly two square cells side-by-side. Preserve the M249 design, black grey white palette and pure red background of image 1. Scale each gun down proportionally so it occupies the lower right of its square frame, with ample empty upper space as in image 2. Gun and hands must touch the bottom edge, no bottom margin. Left idle, right firing with compact white muzzle flash. Same size and positioning in both frames. Fold the bipod alongside the barrel rather than pointing down. Keep broad M249 feed cover and ammo box identifiable. Coarse pixel art for 64x64 per cell. Flat pure red #FF0000 background, no labels, no text. Final sheet MUST be wide landscape 2:1, not square.

Export: split into equal square cells, nearest-neighbour sample to 64x64, convert red key to alpha, quantize to black/128 grey/white, then export with tools/convert_sprite.bat /f. m2490 is idle; m2491 is firing. No gameplay integration changes.
