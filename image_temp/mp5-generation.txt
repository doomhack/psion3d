Generated with the built-in image_gen tool.

Initial prompt:
Use case: stylized-concept. Create a two-frame MP5-style SMG first-person weapon sprite sheet for a monochrome Psion raycaster. Reference images 1 and 2 are the existing PPK idle and firing sprites: match their coarse low-resolution pixel art, sparse black/medium-grey/white palette, perspective, and lower-right anchoring. Reference image 3 is an older MP5 sheet for subject guidance only. Output exactly two equal square cells side by side, idle left and firing right, no margins between cells; intended logical resolution 128x64 total, each frame 64x64. Transparent background, genuine alpha, no checkerboard. In each cell the gun and hands enter from bottom right and the barrel points diagonally toward the upper left/centre, recognisable MP5 receiver, hooded front sight, short barrel and compact fore-end. Hands in the same simple grey/black style as PPK. Keep large upper left negative space. Same gun size, angle and anchored hand placement across both frames, firing frame adds a small white/grey muzzle flash and very slight recoil. Crisp chunky pixel clusters designed to read at 64x64, strictly black #000000, grey #808080 and white #ffffff with transparency, no smooth shading, no antialiasing, no fine high-resolution detail, no text, no labels, no borders, no extra frames, no scene.

Correction prompt:
Edit this sprite sheet: preserve exactly the two guns, hands, positions, monochrome pixel art, framing and white muzzle flash interior. Replace ALL background checkerboard and empty background with flat solid pure red #FF0000, also in holes and between hands; this red is the engine's transparency key. No checkerboard remains. Keep white only inside the muzzle flash and gun highlights. Exactly two equal square cells side by side. No red on the gun or hands. Crisp pixel art, black grey white objects. No text.

Export: split into two equal cells, nearest-neighbour sample to 64x64, convert red key to alpha, quantize to black/128 grey/white, then run tools/convert_sprite.bat /f for each frame. Frames 0 and 1 are idle and firing. No gameplay integration changes.
